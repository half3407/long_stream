## 程序必备功能
1、配置文件设置+读取 （日志等级、日志文件输出地址、服务host、服务端口号、。。。。）
2、日志输出 + 日志文件输出
3、统一的请求/响应格式
[可选]4、请求鉴权，实现权限控制 (老技术:session/cookie,新技术:jwt/oauth1或2)
[可选，但最好有]5、docker + linux

## 功能
1、借阅图书、归还图书
2、增删改查图书信息

## 数据模型定义

图书
图书的自主定义的数据结构
- 书名
- 简介
- 作者
- 内容
一般情况下数据结构的固定结构
- id
- 创建时间(created_date)
- 更新时间(updated_date)
- 删除时间(deleted_date)


统一的请求/响应格式（
